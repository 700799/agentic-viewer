{
  "name": "agent-canvas",
  "version": "0.1.0",
  "private": true,
  "description": "A visual, Airflow-style canvas for observing Claude Code and AI-agent workflows",
  "license": "Apache-2.0",
  "scripts": {
    "dev": "pnpm --filter @agentcanvas/frontend dev",
    "build": "pnpm --filter @agentcanvas/frontend build",
    "lint": "pnpm --filter @agentcanvas/frontend lint"
  },
  "packageManager": "pnpm@10.33.0",
  "pnpm": {
    "onlyBuiltDependencies": ["esbuild"]
  }
}
